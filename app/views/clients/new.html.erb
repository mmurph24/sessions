<%= form_for(@client) do |f| %>
  <div class="field">
    <%= f.label :first_name %><br>
    <%= f.text_area :content %>
  </div>
  <div class="actions">
    <%= f.submit "Create" %>
  </div>
<% end %>
